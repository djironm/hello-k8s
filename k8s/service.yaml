apiVersion: v1
kind: Service
metadata:
  name: hello-k8s
spec:
  type: LoadBalancer

  selector:
    app: hello-k8s

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
